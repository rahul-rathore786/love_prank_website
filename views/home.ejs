<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <link rel="stylesheet" href="style.css" />
    <title>Do you Love ❤️ Me ?</title>
    <style>
      /* Add CSS for header animation here */
      .header {
        font-size: 20px;
        font-family: system-ui, -apple-system, BlinkMacSystemFont, "Segoe UI",
          Roboto, Oxygen, Ubuntu, Cantarell, "Open Sans", "Helvetica Neue",
          sans-serif;
        /* font-weight: bold; */
        position: fixed;
        top: 0;
        left: 0;
        width: 100%;
        /* background-color: #333; */
        color: white;
        text-align: center;
        padding: 10px 0;
        animation: headerSlide 3s ease infinite alternate;
      }

      @keyframes headerSlide {
        0% {
          transform: translateY(0);
        }
        100% {
          transform: translateY(20px);
        }
      }
    </style>
  </head>
  <body>
    <!-- Header -->
    <div class="header">
      This website is developed by Rahul Rathore just for fun purposes
    </div>
    <div class="formdiv">
      <form id="nameForm" method="post" action="/" onsubmit="openFullscreen()">
        <input
          type="text"
          id="nameInput"
          placeholder="Enter your name"
          name="name"
          required
        />
        <button type="submit">Submit</button>
      </form>
    </div>
    <div class="love-box" id="loveBox" style="display: none">
      <p class="love-text" id="loveBoxp">Do You Love ❤️ Me</p>
      <button class="btn" id="yesBtn" onclick="LoveMessage(); hideNoButton()">
        YES
      </button>
      <button
        class="btn"
        id="noBtn"
        onmouseover="moveNoButton(); noMessage()"
        onclick="moveNoButton()"
      >
        NO
      </button>
    </div>
  </body>
  <script src="script.js"></script>
</html>
